Descarga las resoluciones de la Intendencia de Montevideo (http://www.montevideo.gub.uy/aplicacion/resoluciones) en formato `jsonl`.

Para correr:
```
$ python scraper.py resolutions.jsonl --limit=50 --concurrency=2
```